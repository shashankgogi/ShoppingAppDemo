class Product {
  constructor(id, userId, title, image, description, price) {
    this.id = id;
    this.userId = userId;
    this.title = title;
    this.image = image;
    this.price = price;
    this.description = description;
  }
}

export default Product;
